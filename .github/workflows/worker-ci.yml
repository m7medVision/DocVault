name: Worker CI

on:
  push:
    branches: [main]
    paths:
      - 'worker/**'
      - 'shared/**'
  pull_request:
    branches: [main]
    paths:
      - 'worker/**'
      - 'shared/**'

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Lint
        working-directory: worker
        run: |
          go install golang.org/x/lint/golint@latest
          golint ./...
          go vet ./...

      - name: Test
        working-directory: worker
        run: go test ./... -v -race

  build:
    needs: lint-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t docvault-worker:${{ github.sha }} ./worker
